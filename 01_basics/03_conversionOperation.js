let score = 33;
let scoreString = "33";
let string = "98kd";
let newScore = null;
let programmingScore = undefined;
let isProgrammer = true;
let something;

console.table([
  score,
  scoreString,
  string,
  newScore,
  programmingScore,
  isProgrammer,
  something,
]);

let scoreNumber = Number(score);
let scoreStringNumber = Number(scoreString);
let stringNumber = Number(string);
let newScoreNumber = Number(newScore);
let programmingScoreNumber = Number(programmingScore);
let isProgrammerNumber = Number(isProgrammer);
let somethingNumber = Number(something);

console.table([
  scoreNumber,
  scoreStringNumber,
  stringNumber,
  newScoreNumber,
  programmingScoreNumber,
  isProgrammerNumber,
  somethingNumber,
]);
